/*
 * Intentionally minimal translation unit.
 *
 * The fixed-size matrix/vector helpers live inline in `Math.h` so they can be
 * optimized away across translation units while still being shared by C++ code.
 */

