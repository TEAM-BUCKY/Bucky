// Static single-page-app build: render entirely on the client (no SSR runtime),
// and don't prerender — the app talks to the backend at runtime.
export const ssr = false;
export const prerender = false;
