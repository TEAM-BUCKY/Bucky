.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

CMakeFiles
build

.idea

.claude

checkpoints
runs
state
# The bare patterns above ignore the backend's runtime training artifacts
# (simulation/backend/{checkpoints,runs,state}). Re-include the frontend's SOURCE
# state directory, which the bare `state` pattern would otherwise swallow.
!simulation/frontend/src/lib/state/

.svelte-kit
node_modules

# Python envs / caches
.venv
.ruff_cache
.pytest_cache
__pycache__/
*.pyc

# Deployment secrets & state
simulation/.env
simulation/data/
simulation/traefik/letsencrypt/acme.json

# Trained model exports
*.onnx
*.onnx.data

.playwright-mcp

.env
